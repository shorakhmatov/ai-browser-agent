# AI Browser Agent

Автономный AI-агент для управления веб-браузером и выполнения сложных многошаговых задач. Агент самостоятельно анализирует веб-страницы, принимает решения и выполняет действия без предварительно заготовленных сценариев.

## 🎯 Ключевые особенности

- **Полная автономность** - агент сам определяет, что делать дальше, без заготовленных сценариев
- **Видимый браузер** - наблюдайте в реальном времени, как агент взаимодействует с сайтом
- **Интеллектуальное принятие решений** - анализ содержимого страницы и автоматический выбор действий
- **Обработка ошибок** - адаптивное поведение и автоматический retry при неудачах
- **Безопасность** - запрос подтверждения перед деструктивными действиями (delete, pay, submit)
- **Оптимизация контекста** - умное управление ограничениями по токенам
- **Persistent sessions** - сохранение состояния браузера между задачами

## 📋 Требования

- Python 3.8+
- Anthropic API ключ (Claude 3.5 Sonnet)
- 500MB свободного места (для браузера)

## ⚡ Быстрый старт (5 минут)

### 1. Установка
```bash
git clone https://github.com/yourusername/ai-browser-agent.git
cd ai-browser-agent
pip install -r requirements.txt
playwright install
```

### 2. Настройка
```bash
cp .env.example .env
# Отредактируйте .env и добавьте ANTHROPIC_API_KEY
```

### 3. Запуск
```bash
# Интерактивный режим
python cli.py

# Одна задача
python cli.py "найти цену на amazon.com"
```

## 🎮 Использование

### Интерактивный режим
```bash
python cli.py
```

Команды:
- `task <описание>` - выполнить задачу
- `screenshot <путь>` - сделать скриншот
- `url` - получить текущий URL
- `help` - справка
- `exit` - выход

### Примеры задач
```
"Найди форму регистрации и заполни её"
"Перейди на github.com и найди trending repositories"
"Добавь товар в корзину и оформи заказ"
"Найди контактную информацию компании"
```

### Использование в коде
```python
import asyncio
from advanced_agent import AdvancedAIAgent

async def main():
    agent = AdvancedAIAgent("your-api-key")
    await agent.initialize()
    
    result = await agent.execute_task("ваша задача")
    print(result)
    
    await agent.close()

asyncio.run(main())
```

## 🏗️ Архитектура

### Компоненты
- **BrowserController** - управление браузером (Playwright)
- **AdvancedAIAgent** - основной агент с логикой принятия решений
- **ContextManager** - оптимизация контекста для API
- **ErrorHandler** - обработка ошибок и retry логика
- **CLI** - интерфейс командной строки

### Инструменты агента
- `navigate(url)` - переход на URL
- `click(selector)` - клик по элементам
- `type(selector, text)` - ввод текста
- `scroll(direction, amount)` - прокрутка
- `wait(seconds)` - ожидание
- `extract_text()` - получить текст
- `get_elements()` - список элементов
- `screenshot(path)` - скриншот
- `wait_for_element(selector)` - ожидание элемента

## 🔒 Безопасность

Агент запрашивает подтверждение перед:
- Отправкой форм (submit)
- Удалением элементов (delete)
- Оплатой (pay, checkout)
- Другими критическими операциями

## 📚 Документация

- [QUICK_START.md](QUICK_START.md) - быстрый старт
- [ARCHITECTURE.md](ARCHITECTURE.md) - архитектура системы
- [USAGE_EXAMPLES.md](USAGE_EXAMPLES.md) - примеры использования
- [TECHNICAL_DETAILS.md](TECHNICAL_DETAILS.md) - технические детали
- [DEPLOYMENT.md](DEPLOYMENT.md) - развертывание
- [TROUBLESHOOTING.md](TROUBLESHOOTING.md) - решение проблем
- [FEATURES.md](FEATURES.md) - полный список функций

## 🚀 Примеры

### Поиск информации
```bash
python cli.py "Найди цену iPhone 15 на amazon.com"
```

### Заполнение форм
```bash
python cli.py "Заполни контактную форму с данными"
```

### Навигация и анализ
```bash
python cli.py "Перейди на github.com и найди top 5 trending repositories"
```

## 📊 Как это работает

```
1. Пользователь отправляет задачу
   ↓
2. Агент анализирует текущую страницу
   ↓
3. Claude API генерирует инструменты
   ↓
4. Агент выполняет инструменты (click, type, scroll)
   ↓
5. Получает результаты и анализирует изменения
   ↓
6. Повторяет до завершения задачи
```

## ⚙️ Конфигурация

Отредактируйте `config.py` для настройки:
- Максимум итераций (по умолчанию 20)
- Максимум токенов (по умолчанию 8000)
- Деструктивные действия
- Таймауты и задержки

## 🐳 Docker

```bash
docker build -t ai-browser-agent .
docker run -it --env-file .env ai-browser-agent
```

## 📈 Производительность

- Среднее время выполнения: 30-120 секунд
- Поддерживает параллельное выполнение
- Оптимизировано для быстрого выполнения

## ⚠️ Ограничения

- Максимум 20 итераций на задачу
- Контекст ограничен 8000 токенов
- Требует видимого браузера
- Не поддерживает CAPTCHA

## 🤝 Внесение вклада

Смотрите [CONTRIBUTING.md](CONTRIBUTING.md) для инструкций.

## 📄 Лицензия

MIT License - смотрите [LICENSE](LICENSE)

## 🔗 Ссылки

- [Anthropic Claude API](https://www.anthropic.com/)
- [Playwright Documentation](https://playwright.dev/)
- [Python asyncio](https://docs.python.org/3/library/asyncio.html)
